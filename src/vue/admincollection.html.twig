{% extends "administrationbase.html.twig" %}
{% block title %}
  {{ parent() }}
  collection
{% endblock %}

{% block contenu %}

  <div class="container-fluid" style="margin-top:1em">
    <div class="row" style="padding:1em">

      <h4>Liste des collection</h4>
      {%  if liste %}
      <a href="index.php?page=ajoutcollection">
        <button type="button" name="button" class="btnplus" style="  position: absolute;
    right: 20px;">Ajouter</button>
      </a>
        {% endif %}
      <div class="table-responsive">
        <form action="index.php?page=admincollection" method="post" enctype="application/x-www-form-urlencoded">
          <table class="table">
            <thead class="thead-dark">
              <tr>
                <th scope="col">Id</th>
                <th scope="col">Collection</th>
                <th scope="col">Modif</th>
                <th scope="col">Trash</th>
                <th></th>
                <!-- <th >
                  <label class="checkbox">
                    <input type="checkbox"  id="select_all" />
                    <span class="default"></span>
                  </label></th > -->
              </tr>
            </thead>
            <tbody>
              {%  if liste %}
              {% for c in liste %}

                <tr>
                  <th scope="row">#{{ c.idcollection }}</th>
                  <td>{{ c.libellecollection }}</td>
                  <td>
                    
                    <a href="index.php?page=modifcollection&id={{c.idcollection}}">
                      <i class="fas fa-edit icon"></i>
                    </a>
                  </td>
                  <td>
                    <a href="index.php?page=admincollection&id={{c.idcollection}}">
                      <i class="fas fa-trash icon"></i>
                    </a>

                  </td>
                  <td><label class="checkbox">
                    <input type="checkbox" name="cocher[]" id="cocher[]" value="{{c.idcollection}}"/>
                    <span class="default"></span>
                  </label></td>
                </tr>

              {% endfor %}
              {% else %}
              <tr>
                <th scope="row">#</th>
                <td>Vide</td>
                <td>Vide</td>
                <td>Vide</td>
                <td></td>
              </tr>
{% endif %}
            </tbody>
          </table>
          <hr/>
          {%  if liste %}
          <button class="btn btn-mg btn-dark btn-block" type="submit" name="btSupprimer">Supprimer</button>
          {% else %}
          <button style="  width: 100%;" class="btn btn-mg btnplus btn-block" type="submit" name="button"  formaction="index.php?page=ajoutcollection">Ajouter</button>

          {% endif %}
        </form>
      </div>
    </div>
  </div>
{% endblock %}
